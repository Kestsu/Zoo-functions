const data = require('../data/zoo_data');

// const localização = {
//   NE: ['lions', 'giraffes'],
//   NW: ['tigers', 'bears', 'elephants'],
//   SE: ['penguins', 'otters'],
//   SW: ['frogs', 'snakes'],
// }

function getAnimalMap(options) {
  // seu código aqui
//  if (!options) return localização
//  const semInclude = options.sex;
//  if (semInclude = 'female') {
//    return localização
//  }
}
// console.log( getAnimalMap({ sex: 'female' }));
module.exports = getAnimalMap;
